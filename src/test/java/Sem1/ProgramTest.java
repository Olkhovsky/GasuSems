package Sem1;

import org.junit.Assert;
import org.junit.Test;

import java.lang.reflect.Array;

import static org.junit.Assert.*;

public class ProgramTest {
    @Test
    public class CatBoris() {
        String[1] array;
        Assert.assertArrayEquals("Boris can eat milk, red apple", Program.main(new Strzing[0]));
    }
}